
Функция ЭкспортИзображенийВлокальныйКаталог(ТекстОшибки, ЛокальныйКаталог, ТолькоНеВыгруженные, МассивСсылок = Неопределено) Экспорт 
	Результат = Истина;
	
	МассивДанных = Web1C_УправлениеКонтентом.ПолучитьМассивИзображений(ТолькоНеВыгруженные, МассивСсылок);
	
	Для каждого Данные Из МассивДанных Цикл
		Попытка
			ДД = ПолучитьИзВременногоХранилища(Данные.Адрес);
			ДД.Записать(ЛокальныйКаталог + "\" + Данные.ИмяФайла);
			Если Web1C_УправлениеКонтентом.УстановитьОтметкуОВыгрузке(Данные.Ссылка) Тогда
				Сообщить("Объект " + Данные.Ссылка + " успешно выгружен в " + ЛокальныйКаталог);
			КонецЕсли;
			УдалитьИзВременногоХранилища(Данные.Адрес);
		Исключение
		    Результат = Ложь;
			ТекстОшибки = ТекстОшибки + ОписаниеОшибки() + Символы.ПС;
		КонецПопытки;
	КонецЦикла;
	
	Возврат Результат;
КонецФункции

Функция ЭкспортСтилейВлокальныйКаталог(ТекстОшибки, ЛокальныйКаталог, ТолькоНеВыгруженные, МассивСсылок = Неопределено) Экспорт 
	Результат = Истина;
	
	МассивДанных = Web1C_УправлениеКонтентом.ПолучитьМассивСтилей(ТолькоНеВыгруженные, МассивСсылок);
	
	Для каждого Данные Из МассивДанных Цикл
		Попытка
			ТекстСтиля = Данные.Текст;
			Web1C_ФормированиеОтветов.ОбработатьТекстСтраницы(ТекстСтиля, Неопределено);
			
			ТД = Новый ТекстовыйДокумент;
			ТД.УстановитьТекст(ТекстСтиля);
			ТД.Записать(ЛокальныйКаталог + "\" + Данные.ИмяФайла, КодировкаТекста.UTF8);
			Если Web1C_УправлениеКонтентом.УстановитьОтметкуОВыгрузке(Данные.Ссылка) Тогда
				Сообщить("Объект " + Данные.Ссылка + " успешно выгружен в " + ЛокальныйКаталог);
			КонецЕсли;
		Исключение
		    Результат = Ложь;
			ТекстОшибки = ТекстОшибки + ОписаниеОшибки() + Символы.ПС;
		КонецПопытки;
	КонецЦикла;
	
	Возврат Результат;
КонецФункции

Функция ЭкспортСкриптовВлокальныйКаталог(ТекстОшибки, ЛокальныйКаталог, ТолькоНеВыгруженные, МассивСсылок = Неопределено) Экспорт 
	Результат = Истина;
	
	МассивДанных = Web1C_УправлениеКонтентом.ПолучитьМассивСкриптов(ТолькоНеВыгруженные, МассивСсылок);
	
	Для каждого Данные Из МассивДанных Цикл
		Попытка
			ТекстСкрипта = Данные.Текст;
			Web1C_ФормированиеОтветов.ОбработатьТекстСтраницы(ТекстСкрипта, Неопределено);
			
			ТД = Новый ТекстовыйДокумент;
			ТД.УстановитьТекст(ТекстСкрипта);
			ТД.Записать(ЛокальныйКаталог + "\" + Данные.ИмяФайла, КодировкаТекста.UTF8);
			Если Web1C_УправлениеКонтентом.УстановитьОтметкуОВыгрузке(Данные.Ссылка) Тогда
				Сообщить("Объект " + Данные.Ссылка + " успешно выгружен в " + ЛокальныйКаталог);
			КонецЕсли;
		Исключение
		    Результат = Ложь;
			ТекстОшибки = ТекстОшибки + ОписаниеОшибки() + Символы.ПС;
		КонецПопытки;
	КонецЦикла;
	
	Возврат Результат;
КонецФункции

Функция ЭкспортФайловВлокальныйКаталог(ТекстОшибки, ЛокальныйКаталог, ТолькоНеВыгруженные, МассивСсылок = Неопределено) Экспорт 
	Результат = Истина;
	
	МассивДанных = Web1C_УправлениеКонтентом.ПолучитьМассивФайлов(ТолькоНеВыгруженные, МассивСсылок);
	
	Для каждого Данные Из МассивДанных Цикл
		Попытка
			Если Данные.ТекстовыйФайл Тогда
				ТекстСкрипта = Данные.Текст;
				Web1C_ФормированиеОтветов.ОбработатьТекстСтраницы(ТекстСкрипта, Неопределено);
				
				ТД = Новый ТекстовыйДокумент;
				ТД.УстановитьТекст(ТекстСкрипта);
				ТД.Записать(ЛокальныйКаталог + "\" + Данные.ИмяФайла, КодировкаТекста.UTF8);
			Иначе
				ДД = ПолучитьИзВременногоХранилища(Данные.Адрес);
				ДД.Записать(ЛокальныйКаталог + "\" + Данные.ИмяФайла);
			КонецЕсли;
			
			Если Web1C_УправлениеКонтентом.УстановитьОтметкуОВыгрузке(Данные.Ссылка) Тогда
				Сообщить("Объект " + Данные.Ссылка + " успешно выгружен в " + ЛокальныйКаталог);
			КонецЕсли;
			УдалитьИзВременногоХранилища(Данные.Адрес);
		Исключение
		    Результат = Ложь;
			ТекстОшибки = ТекстОшибки + ОписаниеОшибки() + Символы.ПС;
		КонецПопытки;
	КонецЦикла;
	
	Возврат Результат;
КонецФункции

Функция ЭкспортИзображенияВлокальныйКаталог(Ссылка, ТекстОшибки, ЛокальныйКаталог) Экспорт 
	Результат = Истина;
	ЛокКаталог = Новый Файл(ЛокальныйКаталог);
	Если Не ЛокКаталог.Существует() Тогда
		СоздатьКаталог(ЛокальныйКаталог);
	КонецЕсли;
	
	Данные = Web1C_УправлениеКонтентом.ПолучитьДанныеИзображения(Ссылка);
	Попытка
		ДД = ПолучитьИзВременногоХранилища(Данные.Адрес);
		ДД.Записать(ЛокальныйКаталог + "\" + Данные.ИмяФайла);
		Если Web1C_УправлениеКонтентом.УстановитьОтметкуОВыгрузке(Данные.Ссылка) Тогда
			//Сообщить("Объект " + Данные.Ссылка + " успешно выгружен в " + ЛокальныйКаталог);
		КонецЕсли;
		УдалитьИзВременногоХранилища(Данные.Адрес);
	Исключение
		Результат = Ложь;
		ТекстОшибки = ТекстОшибки + ОписаниеОшибки() + Символы.ПС;
	КонецПопытки;
	
	Возврат Результат;
КонецФункции

Функция ЭкспортСтиляВлокальныйКаталог(Ссылка, ТекстОшибки, ЛокальныйКаталог) Экспорт 
	Результат = Истина;
	ЛокКаталог = Новый Файл(ЛокальныйКаталог);
	Если Не ЛокКаталог.Существует() Тогда
		СоздатьКаталог(ЛокальныйКаталог);
	КонецЕсли;
	
	Данные = Web1C_УправлениеКонтентом.ПолучитьДанныеСтиля(Ссылка);
		Попытка
			ТекстСтиля = ПолучитьИзВременногоХранилища(Данные.Текст);
			Web1C_ФормированиеОтветов.ОбработатьТекстСтраницы(ТекстСтиля, Неопределено);
			
			ТД = Новый ТекстовыйДокумент;
			ТД.УстановитьТекст(ТекстСтиля);
			ТД.Записать(ЛокальныйКаталог + "\" + Данные.ИмяФайла, КодировкаТекста.UTF8);
			Если Web1C_УправлениеКонтентом.УстановитьОтметкуОВыгрузке(Данные.Ссылка) Тогда
				//Сообщить("Объект " + Данные.Ссылка + " успешно выгружен в " + ЛокальныйКаталог);
			КонецЕсли;
		Исключение
			Результат = Ложь;
			ТекстОшибки = ТекстОшибки + ОписаниеОшибки() + Символы.ПС;
		КонецПопытки;
	
	Возврат Результат;
КонецФункции

Функция ЭкспортСкриптаВлокальныйКаталог(Ссылка, ТекстОшибки, ЛокальныйКаталог) Экспорт 
	Результат = Истина;
	ЛокКаталог = Новый Файл(ЛокальныйКаталог);
	Если Не ЛокКаталог.Существует() Тогда
		СоздатьКаталог(ЛокальныйКаталог);
	КонецЕсли;
	
	Данные = Web1C_УправлениеКонтентом.ПолучитьДанныеСкрипта(Ссылка);
	Попытка
		Если Данные.ТекстовыйФайл Тогда
			Текст = ПолучитьИзВременногоХранилища(Данные.Текст);
			Web1C_ФормированиеОтветов.ОбработатьТекстСтраницы(Текст, Неопределено);
			
			ТД = Новый ТекстовыйДокумент;
			ТД.УстановитьТекст(Текст);
			ТД.Записать(ЛокальныйКаталог + "\" + Данные.ИмяФайла, КодировкаТекста.UTF8);
		Иначе
			ДД = ПолучитьИзВременногоХранилища(Данные.Адрес);
			ДД.Записать(ЛокальныйКаталог + "\" + Данные.ИмяФайла);
		КонецЕсли;
		
		Если Web1C_УправлениеКонтентом.УстановитьОтметкуОВыгрузке(Данные.Ссылка) Тогда
			//Сообщить("Объект " + Данные.Ссылка + " успешно выгружен в " + ЛокальныйКаталог);
		КонецЕсли;
	Исключение
		Результат = Ложь;
		ТекстОшибки = ТекстОшибки + ОписаниеОшибки() + Символы.ПС;
	КонецПопытки;
	
	Возврат Результат;
КонецФункции

Функция ЭкспортФайлаВлокальныйКаталог(Ссылка, ТекстОшибки, ЛокальныйКаталог) Экспорт 
	Результат = Истина;
	ЛокКаталог = Новый Файл(ЛокальныйКаталог);
	Если Не ЛокКаталог.Существует() Тогда
		СоздатьКаталог(ЛокальныйКаталог);
	КонецЕсли;
	
	Данные = Web1C_УправлениеКонтентом.ПолучитьДанныеФайла(Ссылка);
	Попытка
		Если Данные.ТекстовыйФайл Тогда
			Текст = ПолучитьИзВременногоХранилища(Данные.Текст);
			Web1C_ФормированиеОтветов.ОбработатьТекстСтраницы(Текст, Неопределено);
			
			ТД = Новый ТекстовыйДокумент;
			ТД.УстановитьТекст(Текст);
			ТД.Записать(ЛокальныйКаталог + "\" + Данные.ИмяФайла, КодировкаТекста.UTF8);
		Иначе
			ДД = ПолучитьИзВременногоХранилища(Данные.Адрес);
			ДД.Записать(ЛокальныйКаталог + "\" + Данные.ИмяФайла);
		КонецЕсли;
		
		Если Web1C_УправлениеКонтентом.УстановитьОтметкуОВыгрузке(Данные.Ссылка) Тогда
			//Сообщить("Объект " + Данные.Ссылка + " успешно выгружен в " + ЛокальныйКаталог);
		КонецЕсли;
		УдалитьИзВременногоХранилища(Данные.Адрес);
	Исключение
		Результат = Ложь;
		ТекстОшибки = ТекстОшибки + ОписаниеОшибки() + Символы.ПС;
	КонецПопытки;
	
	
	Возврат Результат;
КонецФункции
